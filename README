Introduction
============

The ``cfg`` and ``py`` files here are the configuration and core of the
*buildout* system for `GroupServer`_. Buildout is used to download the
components of GroupServer, to install them [#eggs]_, and to create the 
initial GroupServer site. The contents of this directory is bundled up
and placed on `the downloads page for GroupServer`_.

Further documentation and copyright information is in the ``docs/``
directory.

..  [#eggs] The components are installed in a subdirectory called 
    ``eggs``.
..  _GroupServer: http://groupserver.org/
..  _The Downloads page for GroupServer: http://groupserver.org/downloads

